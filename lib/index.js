/**
 * dsh-deepseek-peak-indicator — node half.
 *
 * Pure browser-side capability: the empty apply exists so the row appears in
 * the host loader, while the browser half ships through exports["./client"]
 * (same pattern as @deepseek-ai/dsh-cordis-client-runner).
 */
export function apply() {}
